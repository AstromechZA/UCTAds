- title 'Gallery'

.page-header
  %h2
    = 'Gallery'
    %small
      = @advert.title

%p
  = pluralize(@uploads.length, 'Image')

- @uploads.each do |upload|
  = link_to image_tag(upload.image.url(:thumb), class: 'img-thumbnail', alt: upload.description), show_upload_path(upload)
